version: '3.6'

services:
  hw7-db:
    image: postgres:12.8
    container_name: hw7-db
    build: ./flyway
    ports:
      - "5439:5432"
    environment:
      - POSTGRES_DB=hw7
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    healthcheck:
      test: ["CMD", "pg_isready", "-q", "-U", "postgres"]
      interval: 5s
      timeout: 1s
      retries: 2

  flyway:
    image: flyway/flyway
    command: -url=jdbc:postgresql://hw7-db:5432/hw7 -schemas=public -user=postgres -password=postgres -connectRetries=30 migrate
    volumes:
      - ./flyway:/flyway/sql
    depends_on:
      - hw7-db
